// Single source of truth for the build label shown bottom-left.
// Bump this on every deploy: it is how you tell at a glance whether the
// browser is running the latest files or a cached older build.
export const VERSION = '8';
